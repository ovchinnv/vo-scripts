* create pdb files from restart files
*

bomblev -4

if @?xplor eq 0 set xplor

read psf card name @{psffile}.psf @{xplor}

read coor dynr curr name @{rstfile}.rst

if @?pdbfile eq 0 set pdbfile @{rstfile}

write coor pdb name @{pdbfile}.pdb

