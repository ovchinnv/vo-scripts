#!/bin/vmd
# wrap trajectory
# assume lattice vectors loaded
############################################################################
#set id [ molinfo top ];
#set nf [molinfo $id get numframes];

puts "Wrapping coordinate frames..."

pbc wrap -compound res -center com -centersel "protein and noh" -all

#for { set i 0 } { $i<$nf } { incr i } {
#}
#############################################################################
